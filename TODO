MODEL:
- remove controller architecture and rework MovableController into model
  -> and let the map loader load paths for every controlled movable entity
- change player character to have states

ENGINE:
- pull out functionality from TiledMapLoader into MapLoader
- add engine initialization via XML files
- provide a way to change how/where animations are loaded (via initialization file)
  -> then pull out functionality from AnimationLoader into generalized superclass
